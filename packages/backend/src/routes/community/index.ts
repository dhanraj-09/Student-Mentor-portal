export { default as assignmentRoutes } from './assignments.js';
export { default as queryRoutes } from './queries.js';
// `resources.ts` is intentionally still a stub — PBL3 has no resources feature.
